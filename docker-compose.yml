version: "3.8"

services:
  db:
    image: postgres:15
    container_name: leaveit-db
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  app:
    build: .
    container_name: leaveit-app
    depends_on:
      - db
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
    volumes:
      - .:/app
    command: ["dotnet", "leave-it-small.dll"]
    labels:
        - "traefik.enable=true"
        - "traefik.http.routers.leaveit-api.rule=Host(`localhost`) &&  PathPrefix(/api)"
        - "traefik.http.routers.leaveit-api.entrypoints=web"
        - "traefik.http.routers.leaveit-redirect.rule=Host(`localhost`)"
        - "traefik.http.routers.leaveit-redirect.entrypoints=web"
        - "traefik.http.routers.leaveit-redirect.priority=1"
        - "traefik.http.services.leaveit.loadbalancer.server.port=5000" 
  
  traefik: 
      image: traefik:v3.5
      command: 
        - "--api.insecure=true"
        - "--providers.docker=true"
        - "--entrypoints.web.address=:80"
      ports:
        - "80:80"
        - "8080:8080"
      volumes:
        - /var/run/docker.sock:/var/run/docker.sock       

volumes:
  postgres_data:
  
